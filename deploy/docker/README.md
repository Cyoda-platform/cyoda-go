# Canonical Docker provisioning

Dockerfile and reference `compose.yaml` for running cyoda-go in
containers. This directory's contents are filled in by the Docker
per-target spec (follow-up work). The shared design lives in
`docs/superpowers/specs/2026-04-16-provisioning-shared-design.md`.
